- name: pull configuration
  #hosts: 192.168.21.128
  hosts: win_server,win_client
  #strategy: debug
  tasks:
    - name: Display the config
      debug:
        msg: "test"
    - name: Ensure git is installed via Chocolatey
      win_chocolatey:
        name: git
        state: present
        
  #  - name: command de test
   #   win_shell: echo 'aze'
  roles:
    - roleTest
